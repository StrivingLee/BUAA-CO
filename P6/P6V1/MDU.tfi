


// Instantiate the module
MDU instance_name (
    .clk(clk), 
    .reset(reset), 
    .HIen(HIen), 
    .LOen(LOen), 
    .SrcA(SrcA), 
    .SrcB(SrcB), 
    .Start(Start), 
    .Busy(Busy), 
    .HI(HI), 
    .LO(LO)
    );


