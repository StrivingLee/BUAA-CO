


// Instantiate the module
GRF instance_name (
    .clk(clk), 
    .reset(reset), 
    .RegWrite(RegWrite), 
    .A1(A1), 
    .A2(A2), 
    .RegAddr(RegAddr), 
    .RegData(RegData), 
    .PC(PC), 
    .RD1(RD1), 
    .RD2(RD2)
    );


